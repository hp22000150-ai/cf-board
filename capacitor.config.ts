import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.contentfactory.cfboard',
  appName: 'CF Board',
  webDir: 'dist-mobile'
};

export default config;
